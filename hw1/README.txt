Scripts to run:

In folder 'mushrooms':
    boost.py
    dt.py
    knn.py
    nn.py
    svm.py

In folder 'diabetes':
    boost.py
    dt.py
    knn.py
    nn.py
    svm.py

All the data is in the folder 'data.'

The preprocessing and splitting of data into training, validation and testing data is done in process_data.py, which is called by
each of the manually run scripts.

aramaswami32-analysis.tex is the tex file for the analysis and includes the png images that are produced by the scripts mentioned above (It is also included as a file in the submission).

The code and data can be found here: https://github.com/arvindr9/ml/tree/master/hw1